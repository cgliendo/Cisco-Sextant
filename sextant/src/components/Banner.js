//Created by bash script
// import styles from './Banner.module.css';
const Banner = (props) => {
    return (
        <header>
            <h1>
                {props.title}
            </h1>
        </header>
        )
    }
    export default Banner;

